<?php

header('Content-Type: application/json');

$app_env = getenv('APP_ENV') ?: '{{ app_env | default("dev") }}';

$data = [
    'status' => 'ok',
    'service' => 'php-fpm',
    'env' => $app_env,
    'php_version' => phpversion(),
    'timestamp' => date('c')
];

echo json_encode($data, JSON_PRETTY_PRINT) . "\n";